// 'application' plugin provides the 'run' task; 'java-library' does not
apply plugin: "application"

sourceSets.main.java.srcDirs = ["src/"]

java {
    sourceCompatibility = JavaVersion.VERSION_11
    targetCompatibility = JavaVersion.VERSION_11
}

// Required by the application plugin — tells 'run' which class has main()
application {
    mainClass = "com.dodgegame.desktop.DesktopLauncher"
}

dependencies {
    implementation project(":core")
    implementation "com.badlogicgames.gdx:gdx-backend-lwjgl3:$gdxVersion"
    implementation "com.badlogicgames.gdx:gdx-platform:$gdxVersion:natives-desktop"
}

jar {
    archiveFileName = "${appName}-desktop-${version}.jar"
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE
    dependsOn configurations.runtimeClasspath
    from { configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) } }
    manifest {
        attributes 'Main-Class': 'com.dodgegame.desktop.DesktopLauncher'
    }
}
